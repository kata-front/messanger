export type LoginForm = {
    email: string;
    password: string;
}

// components/utilities/types.ts
export type RegisterForm = {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
}